import React from 'react';
import './calendar.css';
export interface CalendarProps {
    onClick?: VoidFunction;
}
export declare const CalendarLunar: ({ onClick }: CalendarProps) => React.JSX.Element;
